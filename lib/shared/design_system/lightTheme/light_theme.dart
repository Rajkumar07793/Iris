import 'package:flutter/material.dart';

ThemeData lightThemeData = ThemeData(
  colorScheme: const ColorScheme.light().copyWith(secondary: Colors.grey[200]),
  fontFamily: "Poppins",
  scaffoldBackgroundColor: Colors.white,
  primaryColor: const Color.fromARGB(255, 12, 142, 129),
);
